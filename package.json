{
  "name": "uniter-bundle",
  "version": "1.0.0",
  "description": "Demo of packaging PHP files up with Browserify+Uniter",
  "main": "index.js",
  "scripts": {
    "build:fs": "node build/fs",
    "build": "npm run build:fs && browserify js/app --standalone=demoApp > dist/bundle.js",
    "webpack": "webpack"
  },
  "keywords": [
    "php",
    "uniter",
    "browserify",
    "fs"
  ],
  "author": "asmblah",
  "license": "MIT",
  "dependencies": {
    "json-loader": "^0.5.1",
    "uniter": "^0.3.0"
  },
  "devDependencies": {
    "browserify": "^10.2.0",
    "glob": "^5.0.6",
    "node-libs-browser": "^0.5.0",
    "webpack": "^1.9.7"
  }
}
